const uniqueValuesArray = ['4','a','3','k']

function randomArray (arrayNow, num) {
const newArray = arrayNow.map (el => {
    if (Math.round (Math.random())) {
        return el
    }
    else {
        el.
    }
})
return newArray
}

const endArray = randomArray (uniqueValuesArray,2)
console.log(endArray);